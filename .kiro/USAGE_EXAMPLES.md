# Usage Examples - NotificationActionService\n\n## Import\n\n```typescript\nimport NotificationActionService from '@/services/NotificationActionService';\n```\n\n## Exemple 1: Répondre depuis la notification\n\n```typescript\n// Dans le handler de notification\nif (actionIdentifier === 'reply') {\n  const userText = response.userText; // \"Merci!\"\n  const conversationId = notification.request.content.data.conversationId;\n  const messageId = notification.request.content.data.messageId;\n  \n  try {\n    // Envoyer la réponse\n    await NotificationActionService.handleReply(\n      conversationId,\n      userText,\n      messageId\n    );\n    console.log('Reply sent successfully');\n  } catch (error) {\n    console.error('Failed to send reply:', error);\n  }\n}\n```\n\n## Exemple 2: Marquer comme lu depuis la notification\n\n```typescript\n// Dans le handler de notification\nif (actionIdentifier === 'markAsRead') {\n  const conversationId = notification.request.content.data.conversationId;\n  const messageId = notification.request.content.data.messageId;\n  \n  try {\n    // Marquer comme lu\n    await NotificationActionService.handleMarkAsRead(\n      conversationId,\n      messageId\n    );\n    console.log('Marked as read successfully');\n  } catch (error) {\n    console.error('Failed to mark as read:', error);\n  }\n}\n```\n\n## Exemple 3: Utiliser directement handleNotificationAction\n\n```typescript\n// Pour une action personnalisée\nawait NotificationActionService.handleNotificationAction({\n  action: 'REPLY',\n  conversationId: '69d5353d7f06c531510b92fa',\n  messageId: '69d6448b6e093f43b4629631',\n  replyText: 'Merci pour ton message!'\n});\n```\n\n## Exemple 4: Avec gestion d'erreur complète\n\n```typescript\ntry {\n  await NotificationActionService.handleReply(\n    conversationId,\n    userText,\n    messageId\n  );\n  \n  // Succès\n  console.log('[Notification] Reply sent successfully');\n  \n  // Optionnel: Afficher une notification de succès\n  // showSuccessNotification('Message envoyé');\n  \n} catch (error) {\n  // Erreur\n  console.error('[Notification] Failed to send reply:', error);\n  \n  // Optionnel: Afficher une notification d'erreur\n  // showErrorNotification('Erreur lors de l\\'envoi');\n  \n  // Optionnel: Retry\n  // setTimeout(() => retry(), 5000);\n}\n```\n\n## Exemple 5: Dans le contexte complet de _layout.tsx\n\n```typescript\nimport NotificationActionService from '@/services/NotificationActionService';\n\n// ...\n\nuseEffect(() => {\n  const subscription = Notifications.addNotificationResponseReceivedListener(\n    async (response) => {\n      const { actionIdentifier, notification } = response;\n      const data = notification.request.content.data as any;\n\n      console.log('[Notification Action]', actionIdentifier, data);\n\n      if (actionIdentifier === 'reply') {\n        const userText = (response as any).userText;\n        if (userText && data?.conversationId) {\n          try {\n            await NotificationActionService.handleReply(\n              data.conversationId,\n              userText,\n              data.messageId\n            );\n            console.log('[Notification] Reply sent via notification action');\n          } catch (error) {\n            console.error('[Notification] Reply failed:', error);\n          }\n        }\n      } else if (actionIdentifier === 'markAsRead') {\n        if (data?.conversationId) {\n          try {\n            await NotificationActionService.handleMarkAsRead(\n              data.conversationId,\n              data.messageId\n            );\n            console.log('[Notification] Marked as read via notification action');\n          } catch (error) {\n            console.error('[Notification] Mark as read failed:', error);\n          }\n        }\n      }\n    }\n  );\n\n  return () => subscription.remove();\n}, []);\n```\n\n## Exemple 6: Avec logging détaillé\n\n```typescript\nconst handleNotificationAction = async (action: string, data: any) => {\n  console.log('[NotificationAction] Starting action:', action);\n  console.log('[NotificationAction] Data:', JSON.stringify(data));\n  \n  try {\n    if (action === 'reply') {\n      console.log('[NotificationAction] Sending reply...');\n      await NotificationActionService.handleReply(\n        data.conversationId,\n        data.replyText,\n        data.messageId\n      );\n      console.log('[NotificationAction] Reply sent successfully');\n    } else if (action === 'markAsRead') {\n      console.log('[NotificationAction] Marking as read...');\n      await NotificationActionService.handleMarkAsRead(\n        data.conversationId,\n        data.messageId\n      );\n      console.log('[NotificationAction] Marked as read successfully');\n    }\n  } catch (error) {\n    console.error('[NotificationAction] Error:', error);\n    console.error('[NotificationAction] Stack:', error.stack);\n  }\n};\n```\n\n## Exemple 7: Avec retry automatique\n\n```typescript\nconst handleNotificationActionWithRetry = async (\n  action: string,\n  data: any,\n  maxRetries = 3\n) => {\n  let lastError;\n  \n  for (let i = 0; i < maxRetries; i++) {\n    try {\n      console.log(`[NotificationAction] Attempt ${i + 1}/${maxRetries}`);\n      \n      if (action === 'reply') {\n        await NotificationActionService.handleReply(\n          data.conversationId,\n          data.replyText,\n          data.messageId\n        );\n      } else if (action === 'markAsRead') {\n        await NotificationActionService.handleMarkAsRead(\n          data.conversationId,\n          data.messageId\n        );\n      }\n      \n      console.log('[NotificationAction] Success');\n      return; // Succès\n    } catch (error) {\n      lastError = error;\n      console.error(`[NotificationAction] Attempt ${i + 1} failed:`, error);\n      \n      if (i < maxRetries - 1) {\n        // Attendre avant de réessayer\n        await new Promise(resolve => setTimeout(resolve, 1000 * (i + 1)));\n      }\n    }\n  }\n  \n  // Tous les tentatives ont échoué\n  console.error('[NotificationAction] All retries failed:', lastError);\n  throw lastError;\n};\n```\n\n## Exemple 8: Avec notification de succès/erreur\n\n```typescript\nconst handleNotificationActionWithFeedback = async (\n  action: string,\n  data: any\n) => {\n  try {\n    if (action === 'reply') {\n      await NotificationActionService.handleReply(\n        data.conversationId,\n        data.replyText,\n        data.messageId\n      );\n      // Afficher une notification de succès\n      showNotification('Message envoyé', 'success');\n    } else if (action === 'markAsRead') {\n      await NotificationActionService.handleMarkAsRead(\n        data.conversationId,\n        data.messageId\n      );\n      // Afficher une notification de succès\n      showNotification('Marqué comme lu', 'success');\n    }\n  } catch (error) {\n    console.error('[NotificationAction] Error:', error);\n    // Afficher une notification d'erreur\n    showNotification('Erreur lors du traitement', 'error');\n  }\n};\n```\n\n## Exemple 9: Avec validation des données\n\n```typescript\nconst validateNotificationData = (data: any): boolean => {\n  if (!data?.conversationId) {\n    console.error('[NotificationAction] Missing conversationId');\n    return false;\n  }\n  \n  if (!data?.replyText && data?.action === 'REPLY') {\n    console.error('[NotificationAction] Missing replyText for REPLY action');\n    return false;\n  }\n  \n  return true;\n};\n\nconst handleNotificationActionWithValidation = async (\n  action: string,\n  data: any\n) => {\n  if (!validateNotificationData(data)) {\n    console.error('[NotificationAction] Invalid data');\n    return;\n  }\n  \n  try {\n    if (action === 'reply') {\n      await NotificationActionService.handleReply(\n        data.conversationId,\n        data.replyText,\n        data.messageId\n      );\n    } else if (action === 'markAsRead') {\n      await NotificationActionService.handleMarkAsRead(\n        data.conversationId,\n        data.messageId\n      );\n    }\n  } catch (error) {\n    console.error('[NotificationAction] Error:', error);\n  }\n};\n```\n\n## Exemple 10: Avec analytics\n\n```typescript\nconst trackNotificationAction = (action: string, success: boolean) => {\n  // Envoyer à un service d'analytics\n  analytics.track('notification_action', {\n    action,\n    success,\n    timestamp: new Date().toISOString()\n  });\n};\n\nconst handleNotificationActionWithAnalytics = async (\n  action: string,\n  data: any\n) => {\n  try {\n    if (action === 'reply') {\n      await NotificationActionService.handleReply(\n        data.conversationId,\n        data.replyText,\n        data.messageId\n      );\n    } else if (action === 'markAsRead') {\n      await NotificationActionService.handleMarkAsRead(\n        data.conversationId,\n        data.messageId\n      );\n    }\n    \n    trackNotificationAction(action, true);\n  } catch (error) {\n    console.error('[NotificationAction] Error:', error);\n    trackNotificationAction(action, false);\n  }\n};\n```\n\n## Résumé\n\n### Méthodes Disponibles\n- `handleReply(conversationId, replyText, messageId?)` - Envoyer une réponse\n- `handleMarkAsRead(conversationId, messageId?)` - Marquer comme lu\n- `handleNotificationAction(payload)` - Méthode générique\n\n### Logging\n- `[NotificationAction] Sending action:` - Action envoyée\n- `[NotificationAction] Action processed successfully:` - Succès\n- `[NotificationAction] Failed to process action:` - Erreur\n\n### Gestion des Erreurs\n- Toutes les erreurs sont loggées\n- Pas de crash de l'app\n- Utilisateur peut réessayer\n\n### Bonnes Pratiques\n- Toujours vérifier les données\n- Toujours gérer les erreurs\n- Toujours logger les actions\n- Optionnel: Afficher des notifications de succès/erreur\n- Optionnel: Implémenter un retry automatique\n- Optionnel: Tracker les actions avec analytics\n